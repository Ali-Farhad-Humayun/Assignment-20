    // 4. Promise States and Transitions

    // Question:
// List the states of a Promise and their transitions.

    // Answer:

// Pending: Initial state (neither resolved nor rejected).

// Fulfilled: Transitioned via resolve(value).

// Rejected: Transitioned via reject(error).

// Once settled (fulfilled/rejected), a Promise cannot change state.